/* eslint-disable @next/next/no-img-element */
import { ArrowRight, FileText } from "lucide-react";
import { PageHeading } from "@/components/page-heading";
import { CompanyLogo } from "@/components/company-logo";
import { education, experience } from "@/lib/portfolio";

export default function ExperiencePage(){return <main><PageHeading eyebrow="EXPERIENCE / EDUCATION" title="A career built layer by layer." intro="From applied NLP to full-stack AI, agentic workflows, and production GenAI systems."/><section className="shell timeline experience-timeline">{experience.map((x,i)=><article className="experience-entry" key={x.company}><div className="experience-rail"><CompanyLogo company={x.company} mark={x.mark} src={x.logo}/><span className="timeline-index">0{i+1}</span></div><div className="experience-content"><div className="experience-head"><div><p className="mono">{x.dates} · {x.location}</p><h2>{x.role}</h2><h3>{x.company}</h3></div>{i===0&&<span className="current-badge">CURRENT</span>}</div><p className="experience-detail">{x.detail}</p><p className="experience-transition"><span>PROGRESSION</span>{x.transition}</p><ul className="experience-highlights">{x.highlights.map(point=><li key={point}>{point}</li>)}</ul><div className="experience-links">{i<3&&<a href={`/projects/${projectsForRole[i]}`} className="text-link action-link">Related case study <ArrowRight size={15}/></a>}</div></div></article>)}</section><section className="shell education-section"><p className="kicker">EDUCATION</p><div className="education-list">{education.map(([degree,school,dates,mark,logo])=><article key={degree}><div className="education-mark"><img className="education-logo" src={logo} alt={`${school} logo`} /><span aria-hidden="true">{mark}</span></div><div className="education-copy"><p className="education-dates">{dates}</p><h3>{degree}</h3><p className="education-school">{school}</p></div></article>)}</div></section><section className="shell resume-callout"><div><p className="kicker">RECRUITER VERSION</p><h2>Prefer the concise record?</h2></div><a className="button action-link" href="/resume"><FileText size={17}/> Preview or download résumé</a></section></main>}
const projectsForRole=["regulatory-intelligence-platform","audit-intelligence-platform","infraspeak","mortgage-document-classification"];
