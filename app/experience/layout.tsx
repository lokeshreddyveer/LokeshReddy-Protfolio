import { pageMetadata } from "@/lib/seo";
export const metadata = pageMetadata("/experience", "Experience | Lokesh Reddy V", "Career progression from transformer-based NLP and full-stack AI to bounded agents and production GenAI systems.");
export default function ExperienceLayout({ children }: { children: React.ReactNode }) { return children; }
