import { pageMetadata } from "@/lib/seo";
export const metadata = pageMetadata("/architecture", "Architecture Explorer | Lokesh Reddy V", "Inspect a production GenAI request from policy and retrieval through validation, auditability, and recovery.");
export default function ArchitectureLayout({ children }: { children: React.ReactNode }) { return children; }
