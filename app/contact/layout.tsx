import { pageMetadata } from "@/lib/seo";
export const metadata = pageMetadata("/contact", "Contact | Lokesh Reddy V", "Start a focused conversation about Generative AI engineering, production RAG, agents, evaluation, or AI security.");
export default function ContactLayout({ children }: { children: React.ReactNode }) { return children; }
