import { pageMetadata } from "@/lib/seo";
export const metadata = pageMetadata("/studio", "Engineering Studio | Lokesh Reddy V", "Challenge production GenAI systems with role-fit analysis, architecture decisions, RAG debugging, incident replay, security review, and evidence tools.");
export default function StudioLayout({ children }: { children: React.ReactNode }) { return children; }
