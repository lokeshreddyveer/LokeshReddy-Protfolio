import { pageMetadata } from "@/lib/seo";
export const metadata = pageMetadata("/projects", "Projects | Lokesh Reddy V", "Public-safe case studies from regulated-document intelligence, audit and due diligence workflows, evaluation, model adaptation, infrastructure automation, network operations, and applied NLP.");
export default function ProjectsLayout({ children }: { children: React.ReactNode }) { return children; }
