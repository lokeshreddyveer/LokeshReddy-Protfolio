import { ArrowRight, BarChart3, Layers3 } from "lucide-react";
import { PageHeading } from "@/components/page-heading";
import { projects } from "@/lib/portfolio";

export default function ProjectsPage(){return <main><PageHeading eyebrow="SYSTEMS / CASE STUDIES" title="Production thinking, made inspectable." intro="Eleven public-safe case studies across retrieval, agents, evaluation, infrastructure automation, network operations, and applied NLP. Scan the evidence here; open a case study for the full system story."/><section className="shell project-index-summary"><span><BarChart3 size={17}/><strong>11</strong><small>case studies</small></span><span><Layers3 size={17}/><strong>6</strong><small>system layers</small></span><p>Every page separates the outcome from the implementation details, so the portfolio is easier to evaluate quickly.</p></section><section className="shell project-index-grid">{projects.map((p,i)=><a key={p.slug} href={`/projects/${p.slug}`} className="project-index-card" aria-label={`Open case study: ${p.title}`}><div className="project-index-card-top"><span className="project-index-number">{String(i+1).padStart(2,"0")}</span><span className="project-index-label">{p.label}</span><ArrowRight className="project-index-arrow" size={18}/></div><span className="evidence-label">{p.evidence}</span><h2>{p.title}</h2><p className="project-index-outcome">{p.outcome}</p><div className="project-index-metrics">{p.metrics.slice(0,2).map(([value,label])=><span key={label}><strong>{value}</strong><small>{label}</small></span>)}</div><div className="project-index-footer"><div className="project-index-stack">{p.stack.slice(0,3).map(x=><em key={x}>{x}</em>)}</div><span>Open case study</span></div></a>)}</section></main>}
