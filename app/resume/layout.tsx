import { pageMetadata } from "@/lib/seo";
export const metadata = pageMetadata("/resume", "Resume | Lokesh Reddy V", "Preview or download Lokesh Reddy V's Generative AI Engineer résumé, focused on RAG, agents, evaluation, and LLM security.");
export default function ResumeLayout({ children }: { children: React.ReactNode }) { return children; }
