import { pageMetadata } from "@/lib/seo";
export const metadata = pageMetadata("/lab/agent-trace", "Agent Trace | Lokesh Reddy V", "Inspect a bounded agent workflow through planning, retrieval, tool authorization, verification, and final response validation.");
export default function AgentTraceLayout({ children }: { children: React.ReactNode }) { return children; }
