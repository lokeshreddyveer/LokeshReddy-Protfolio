import { pageMetadata } from "@/lib/seo";
export const metadata = pageMetadata("/lab/rag-debugger", "RAG Debugger | Lokesh Reddy V", "Trace a retrieval-augmented generation request through query rewriting, search, reranking, context selection, and evaluation.");
export default function RagDebuggerLayout({ children }: { children: React.ReactNode }) { return children; }
