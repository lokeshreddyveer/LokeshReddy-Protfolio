import { pageMetadata } from "@/lib/seo";
export const metadata = pageMetadata("/about", "About | Lokesh Reddy V", "How Lokesh Reddy designs accountable GenAI systems through evidence, bounded autonomy, security, and operational discipline.");
export default function AboutLayout({ children }: { children: React.ReactNode }) { return children; }
