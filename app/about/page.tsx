"use client";
import { ArrowRight, CircleCheck } from "lucide-react";
import { PageHeading } from "@/components/page-heading";
import { certifications, fieldNotes, skillGroups } from "@/lib/portfolio";

export default function AboutPage(){const principles=["Accuracy before spectacle","Security as an architecture concern","Evaluation before release","Observable behavior after deployment","Deterministic controls where risk is high","Clear trade-offs over tool collecting"];return <main><PageHeading eyebrow="ABOUT / ENGINEERING PHILOSOPHY" title="I make probabilistic systems operationally accountable." intro="I’m Lokesh Reddy V, a Generative AI Engineer focused on the layers that turn model capability into reliable software."/><section className="shell about-grid"><article><p className="kicker">HOW I THINK</p><h2>The model is one component.</h2><p>Useful GenAI products depend on data boundaries, retrieval quality, tool permissions, evaluation discipline, predictable interfaces, failure recovery, and clear evidence. I design those pieces as a system—not as an afterthought around a prompt.</p><p>My background spans transformer-based NLP, full-stack AI applications, infrastructure knowledge systems, and agentic workflows. That progression shaped a practical bias: build the smallest reliable control loop, measure it, then expand autonomy only where the evidence supports it.</p></article><aside><p className="kicker">ENGINEERING PRINCIPLES</p>{principles.map(x=><div key={x}><CircleCheck/><span>{x}</span></div>)}</aside></section><section className="shell current-focus"><p className="kicker">CURRENT FOCUS</p><h2>Secure RAG · Bounded agents · Evaluation infrastructure · LLM observability</h2><a className="button action-link" href="/projects">See the systems <ArrowRight/></a></section><section className="shell about-skills"><div className="section-heading"><div><p className="kicker">SKILLS</p><h2>The systems toolkit.</h2></div></div><div className="about-skills-grid">{skillGroups.map(([group,skills])=><article key={group}><h3>{group}</h3><p>{skills.join(" · ")}</p></article>)}</div></section><section className="shell about-certifications"><div className="section-heading"><div><p className="kicker">CERTIFICATIONS</p><h2>Credentials that support the work.</h2></div></div><div className="about-certifications-grid">{certifications.map(([name,issuer,year])=><article key={name}><h3>{name}</h3><p>{issuer} · {year}</p></article>)}</div></section><section className="shell about-notes" id="field-notes"><div className="section-heading"><div><p className="kicker">FIELD NOTES</p><h2>Short arguments from the systems layer.</h2></div><a className="text-link" href="/studio?module=readiness">Inspect the controls <ArrowRight/></a></div><div className="about-notes-grid">{fieldNotes.map(note=><a href={note.href} key={note.slug}><span>{note.type}</span><strong>{note.title}</strong><p>{note.summary}</p><small>{note.tags.join(" · ")}</small></a>)}</div></section></main>}
