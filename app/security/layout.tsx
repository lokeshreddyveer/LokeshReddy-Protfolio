import { pageMetadata } from "@/lib/seo";
export const metadata = pageMetadata("/security", "AI Security Lab | Lokesh Reddy V", "Explore synthetic prompt-injection, PII, tool-authorization, trust-boundary, and audit controls for LLM applications.");
export default function SecurityLayout({ children }: { children: React.ReactNode }) { return children; }
